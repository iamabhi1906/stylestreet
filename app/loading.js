import LoadingIcons from "@/components/loading-icons";

const Loading = () => {
  return (
    <div className="fixed h-dvh z-10 flex items-center justify-center w-full">
        <LoadingIcons />
    </div>
  );
};

export default Loading;
